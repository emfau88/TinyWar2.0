{
  "name": "tinywar-html",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:map": "node scripts/generate-map-data.mjs",
    "diagnose:map": "node scripts/diagnose-map.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "npm run generate:map && tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "npm run generate:map && vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^3.2.4"
  }
}
